<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zjz.demo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Demo">

        <activity android:name=".exp5.MusicPlayerActivity" />
        <activity android:name=".exp4.ShowPowerActivity" />
        <activity android:name=".exp3.AddMemberAcitivity" />
        <activity android:name=".exp3.ShowMembersActivity" />
        <activity android:name=".exp2.RegActivity" />
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".exp2.SignActivity" />
    </application>

</manifest>