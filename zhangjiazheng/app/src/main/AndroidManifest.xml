<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.edu.zust.lxy.zhangjiazheng">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Zhangjiazheng">
        <activity android:name=".EditTableActivity"></activity>
        <activity android:name=".LeaveWordActivity" />
        <activity android:name=".EditNoticeActivity" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".Class2Activity" />
        <activity android:name=".Class1Activity" />
        <activity android:name=".ShowUserInfoActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>